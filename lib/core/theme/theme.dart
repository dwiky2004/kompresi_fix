import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1A237E);
  static const Color secondary = Color(0xFFFFC107);

  static const Color background = Color(0xFFF3F4F6);
  static const Color card = Colors.white;

  static const Color textMain = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
}

class AppTheme {
  static const double borderRadius = 16.0;

  // Palette used mainly in history screen and cards
  static const Color primary = AppColors.primary;
  static const Color textMain = AppColors.textMain;
  static const Color textSecondary = AppColors.textSecondary;

  static const Color contentCard = Color(0xFFF9FAFB);
  static const Color surface = Color(0xFFE5E7EB);

  static const Color brandSecond = Color(0xFF00BCD4);
  static const Color success = Color(0xFF22C55E);
  static const Color destructive = Color(0xFFEF4444);
}

